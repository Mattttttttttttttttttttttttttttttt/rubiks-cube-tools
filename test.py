# import re
# a = {1:2, 3:4, 5:6}
# for b, [c, d] in enumerate(list(a.items())):
#     print(b,c,d)

# a = None
# b = {f"ao{a}": 3}
# print(b.keys())

# from avg_generator import avg_str
# from helpers import no_paren
# u = []
# for line in iter(input, ""):
#     u.append(line)
# u = u[-1]
# solves = [no_paren(i) for i in u.split(", ")]
# print(avg_str(len(solves), solves))
